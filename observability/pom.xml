<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>observability-use-cases</artifactId>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>use-cases-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <properties>
        <!--
          Observability Kit (the Micrometer rewrite, 5.0-SNAPSHOT) builds on Flow
          25.3-SNAPSHOT and relies on the SessionLockListener / RpcInvocationListener
          SPIs (Flow PRs #24498 / #24499). Pin this module to the 25.3 line so the
          Flow / components / vaadin artifacts align with the kit; the rest of the
          repo stays on the baseline 25.2-SNAPSHOT.
        -->
        <flow.version>25.3-SNAPSHOT</flow.version>
        <flow-components.version>25.3-SNAPSHOT</flow-components.version>
        <vaadin.version>25.3-SNAPSHOT</vaadin.version>
        <observability-kit.version>5.0-SNAPSHOT</observability-kit.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>use-cases-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-dev</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-spring-boot-starter</artifactId>
        </dependency>
        <!--
          Observability Kit Spring Boot starter. Its auto-configuration pulls in
          Boot's Micrometer metrics support (a MeterRegistry is wired out of the
          box) and registers the Vaadin instrumentation, which records into that
          registry. spring-boot-starter-actuator is kept to expose the metrics
          over /actuator; it is otherwise optional now that the starter provides
          the registry.
        -->
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>observability-kit-starter</artifactId>
            <version>${observability-kit.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>browserless-test-spring</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>spring-boot:run</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.vaadin</groupId>
                <artifactId>flow-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
