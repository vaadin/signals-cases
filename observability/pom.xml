<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>observability-use-cases</artifactId>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>use-cases-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <properties>
        <!--
          The vaadin-micrometer instrumentation (Flow's prototype/vaadin-micrometer
          branch) is built on 25.3-SNAPSHOT. Pin this module to that line so the
          micrometer artifacts align with flow-server / flow-components / vaadin,
          the way other modules pin a feature snapshot. The rest of the repo stays
          on the baseline 25.2-SNAPSHOT.
        -->
        <flow.version>25.3-SNAPSHOT</flow.version>
        <flow-components.version>25.3-SNAPSHOT</flow-components.version>
        <vaadin.version>25.3-SNAPSHOT</vaadin.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>use-cases-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-dev</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-spring-boot-starter</artifactId>
        </dependency>
        <!--
          vaadin-micrometer instrumentation + its Spring Boot auto-configuration.
          VaadinMetricsAutoConfiguration activates when a MeterRegistry bean is
          present; spring-boot-starter-actuator both puts MeterRegistry on the
          classpath and auto-configures a SimpleMeterRegistry to satisfy that.
        -->
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-micrometer-spring-boot</artifactId>
            <version>${flow.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>browserless-test-spring</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>spring-boot:run</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.vaadin</groupId>
                <artifactId>flow-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
