<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>clipboard-use-cases</artifactId>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>use-cases-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <properties>
        <!-- Pinned to the replace-children-temp feature branch of vaadin/flow
             so the use cases can switch back to the new temporal trigger
             actions (ReplaceChildrenTemporarilyAction, SetPropertyTemporarilyAction)
             without bumping again. The branch is a superset of paste-file
             (used by UC5/UC7 paste APIs) and the paste-events branch (UC4
             writeImage). flow-components and vaadin-bom stay on the parent's
             25.2 versions — the branch only publishes flow artifacts, and
             flow-bom is imported first in the parent so its 25.3 entries win
             for flow dependencies. -->
        <flow.version>25.3.replace-children-temp-SNAPSHOT</flow.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>use-cases-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <!-- Replace artifactId with vaadin-core to use only free components -->
            <artifactId>vaadin</artifactId>
            <exclusions>
                <!-- vaadin-board-flow 25.2 has a packaging bug: it ships
                     byte-identical duplicates of flow-server's
                     com.vaadin.flow.component.clipboard.{Clipboard,
                     ClipboardBinding, ClipboardContent} classes and even
                     claims to export the package in its OSGi manifest. With
                     flow-server pinned to 25.3.paste-events-SNAPSHOT, the
                     board jar appears earlier on the classpath and shadows
                     the new clipboard API (writeImage, onPaste, ...). The
                     views here don't use Board, so dropping it is the
                     simplest workaround until the board jar stops bundling
                     classes that aren't its own. -->
                <exclusion>
                    <groupId>com.vaadin</groupId>
                    <artifactId>vaadin-board-flow</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-spreadsheet-flow</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.18.3</version>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-dev</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>browserless-test-spring</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>spring-boot:run</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.vaadin</groupId>
                <artifactId>flow-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
